/**
 * El dominio de taxes/tax-types/tax-lines se maneja en `src/store/api/taxesApi.ts` (RTK Query).
 * Los tipos viven en `src/types/tax.ts`.
 *
 * Este archivo se mantiene vacío para no romper imports históricos. Se puede
 * eliminar una vez verificado que nadie importa de aquí.
 */
export {};
